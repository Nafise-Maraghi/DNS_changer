# Simpe app for setting and flushing DNS

the default DNS is from shecan.ir

you can set your preferred dns by changing the "address" in "primary_dns" and "alternative_dns" variables in the source code.
